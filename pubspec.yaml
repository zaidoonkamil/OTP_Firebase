name: otp
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  country_picker:
  firebase_core:
  firebase_auth:
  cloud_firestore:
  firebase_storage:
  shared_preferences:
  provider:
  pinput:
  image_picker:
  firebase_app_check:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:


  uses-material-design: true

  assets:
    - assets/